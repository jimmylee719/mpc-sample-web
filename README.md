# 敲敲取樣 · 一台就夠

> 全球第一個繁體中文的 Akai MPC Sample 完整教學系統。
> 把一台 US$399 的手持取樣機，變成能上台表演、也能獨立完成整首歌的樂器 —— 全程不需要電腦。

**Repo**：`mpc-sample-web`（private）｜**基準韌體**：1.3.0｜**官方手冊**：v1.3.0 (RevA)

> 韌體與手冊版本於 **2026-09-20** 重新查核：官方 CDN 上仍只有 v1.3.0 (RevA)，
> v1.3.1／v1.4.0 皆為 404。1.3.0（2026-04-13）新增 Normalize 與三種 Knob Takeover，
> 1.2.x 新增免拔卡的 SD 存取、Export Song to Sequence、Loop Lock per Pad。**基準無需調整。**

---

## 1. 現在做到哪裡

| Phase | 交付 | 狀態 |
|---|---|---|
| **P0** | 專案初始化、設計 token、型別、內容驗證腳本 | ✅ 完成 |
| **P1** | `MpcPanel` + `MpcScreen` + `LessonPlayer` + Season 1 | ✅ 完成 |
| **P2** | 查詢區六頁 + Pagefind 搜尋 | ✅ 完成 |
| **P3** | Season 2 八課 + 音檔管線 | ⚠️ 課程完成，**音檔待 R2 憑證** |
| **P4** | Season 3 十課（含補上的自動化） | ✅ 完成 |
| **P5** | Season 4 + Season 5 十三課 | ✅ 完成 |
| **P6** | 曲風工廠 + 素材庫 | ⚠️ 24 張配方卡完成，**素材檔案待錄製** |

**程式部分全部完成。剩下的三件事都需要實體機器或帳號憑證，見 §3。**

---

## 2. 內容統計

執行 `npm run stats` 可以隨時重新產生這份數字。

### 課程：36 課 / 456 步

| Season | 主題 | 課數 | 步數 |
|---|---|---|---|
| S1 | 起步 | 5 | 93 |
| S2 | 取樣工藝 | 8 | 111 |
| S3 | 編曲 | 10 | 118 |
| S4 | 聲音設計 | 7 | 70 |
| S5 | 現場演出 | 6 | 64 |

需要電腦的課只有一堂：**1-4 韌體更新**。其餘 35 課全部在機器上完成。

### 曲風工廠：24 張配方卡

| 級別 | 定義 | 數量 |
|---|---|---|
| L1 | 機上直接完成，0–1 次 resample | 5 |
| L2 | 1–2 次 resample | 9 |
| L3 | 3 次以上，pad 預算吃緊 | 6 |
| L4 | 機上做不完整，頁首強制說明 | 4 |

每張都是固定八段式結構。Amapiano 另含一個迷你播放器，處理 log drum 調音。

> 計畫書原標題「16 頁」為筆誤（下表實際只列 15 個），已於 2026-08-09 修訂為 **24 個**並補齊。

### 查詢區

| 頁面 | 內容量 |
|---|---|
| 快捷鍵總表 | 38 組（含 4 組面板沒印的隱藏功能） |
| 旋鈕矩陣 | 13 個畫面，**全部查證完成** |
| 效果字典 | 44 種 = Pad FX 16 + Knob FX 28 |
| 名詞對照 | 38 組 |
| 疑難排解 | 14 條（只收有官方依據的） |
| 韌體對照 | 版本紀錄 + 6 項尚未驗證 + 4 項已排除（含依據） |

### 延伸觀看影片：100 支（110 則引用）

| 來源 | 支數 | 覆蓋 |
|---|---|---|
| Akai Professional 官方系列 | 30 | 21 個頁面 |
| 社群頻道（NervousCook$ 專門系列等） | 70 | 43 個頁面 |

**36 課全部都有影片。曲風 17/24 有影片**，其餘 7 個（phonk、jersey-club、footwork、
afrobeats、amapiano、rnb、pop）目前找不到用取樣機做的教學，頁面直接寫明沒有，
不拿不相干的影片湊數。

每一支的 ID、標題、頻道都用 YouTube oEmbed 端點驗證過。重跑：`npm run videos:verify`。
覆蓋率報告：`npm run videos`。

**⚠️ 兩個誠實說明**

1. 對應是**依影片標題**判斷主題配上去的，**本站尚未有人實際觀看影片內容**。
   全部標 `reviewed: false`，前台顯示「尚未人工確認」。
2. 不是 MPC Sample 拍的影片一律標示 `device`（MPC One、MPC Live、MPC 軟體⋯⋯），
   前台會出現橘色警告：**觀念可以參考，按鍵位置不要照抄**。
   這是必要的防護，那些機型有 Track Mute、觸控螢幕這些本機沒有的東西。

**字幕**：嵌入時帶 `cc_load_policy=1` 與 `cc_lang_pref=zh-Hant`，播放器會自動開字幕並優先
選繁體中文，包含 YouTube 自己的自動翻譯。

**中文摘要（2026-08-10 開放，目前 0 支已填）**：`summary` 欄位可以放**看過影片的人用自己的話**
寫的 200 字內重點，前台以綠框「中文重點」呈現。逐字稿與整份翻譯字幕仍是紅線。
驗證腳本強制「有摘要就必須 `reviewed: true`」——**Claude 沒有影像與聲音輸入、看不了影片，
不會也不能產生摘要**，這一欄只能由看過的人填。

播放器是**點擊才載入**：按下播放之前，頁面對 YouTube 零請求，一律走 `youtube-nocookie.com`。

**全站 79 個靜態頁面**，Pagefind 建置期索引，搜尋不需要伺服器。

### 學習體驗

2026-08-10 依兩個角色實際走過全站後補的功能：**完全沒碰過機器的小學生**，
以及**要把網站推薦給學生的專業教練**。

| 缺口 | 補法 |
|---|---|
| 課程從「按下電源」開始，開箱、接線、充電完全沒教 | 新增 `/start` 開始之前 |
| 36 課 456 步只看得到總量，看不到自己走到哪 | 課程地圖顯示完成數、進度條、每課狀態、「繼續上次」 |
| 27 步的課要看第 20 步得按 19 次 | 章節條可以點，直接跳到那一段 |
| 老師沒辦法把「第 12 步」丟給學生 | 網址支援 `?step=12`，翻頁時網址跟著走 |
| 搜尋只在查詢區六頁上，卡住的人找不到 | 搜尋移到頂列，全站都有，按 `/` 開啟 |
| 機器上全是英文，新手卡在單字 | 每一課自動列出「這一課會用到的名詞」 |
| 機器不在電腦旁邊，沒辦法照著做 | 步驟表可列印，列印時自動隱藏導覽與播放器 |

### 課文裡的實體按鍵圖示

課文說「按 CHOP」的時候，那顆 <b>CHOP</b> 會直接畫成面板上真正的樣子 ——
同一個顏色、同一個圓角、同一個絲印字體。圖是從 `panel-layout.ts` 的同一份資料
裁下來的，所以永遠不可能跟面板走鐘。

- 全站 **279 段粗體**含可視覺化的按鍵，涵蓋 60 種控制項
- 只換 `<b>` 裡面、且明確指向實體控制項的詞。Threshold、Regions 這種參數名稱不換
- 比對規則前後都有界限判斷，`RESAMPLE` 不會被咬成 RE + SAMPLE
- SVG 標 `aria-hidden` 與 `data-pagefind-ignore`，旁邊放一份螢幕閱讀器與搜尋看得到的文字，
  把文字換成圖不會害搜尋失效
- 稽核指令 `npm run keys`，會列出每一個被換掉的詞，以及沒加粗的按鍵名稱（目前 40 處）

### 介面與 PWA

- **全站頂列**：sticky，含返回鍵。底線是機器上的三個顏色（AKAI 紅 → FX 橘 → PLAY 綠）。
- **返回鍵**：站內走過就用瀏覽歷史；從桌面捷徑直接開在課程頁時改跳「上一層」，
  不會按了沒反應。
- **行動裝置底列**：五個分頁（首頁／課程／曲風／查詢／素材），含安全區內距。
- **可安裝**：`manifest.webmanifest`、standalone 顯示、三個捷徑、自製 PNG 圖示
  （`npm run icons`，用 Node 內建 zlib 直接產 PNG，沒有裝任何影像套件）。
- **離線**：Service Worker 頁面一律 network-first，只在斷線時退回快取，
  完全沒開過的頁面顯示本站自己的 `/offline`，不是瀏覽器錯誤頁。
- **音樂元素**：跳動電平表、錄音燈、旋轉黑膠、pad 矩陣、波形分隔線——
  全部純 CSS，沒有任何動畫函式庫，且尊重 `prefers-reduced-motion`。
- **插畫**：唱盤（俯視，含唱臂與速度推桿）與唱片箱，全部手繪 SVG 路徑。
  首頁另有整台機器的向量面板。**一張產品照片都沒有用**——Akai 的照片有著作權，
  而且點陣圖會拖垮 LCP。
- **法務頁**：`/privacy` 隱私權政策（只寫實際會發生的事）、`/about` 關於本站
  （凡圖有限公司資訊、事實查核政策、免責與商標聲明）。

---

## 3. 還沒完成的三件事

這三件我做不到，需要你。

### ① Cloudflare R2 憑證

音檔播放器（`AudioClip`、`AudioAB`）程式已完成，`preload="none"` 不影響載入速度。
缺的是一個環境變數：

```
NEXT_PUBLIC_R2_BASE=https://你的網域
```

複製 `.env.example` 成 `.env.local` 再填。上傳用的憑證只在本機或 CI 使用，**永遠不進版控**。
在設定之前，播放器會誠實顯示「音檔尚未上線」，而不是壞掉的播放器。

### ② 素材錄製

素材庫的政策與四個分類都建好了，但**檔案必須真的錄下來，不能生成**。
規劃中的分類：生活打擊、環境音、質感層、手作沙鈴。

### ③ 實機驗證 35 課

- **1-2「你的第一個 30 分鐘」** 出自 `reference/prototype-v2.html`，視為已驗證
- **其餘 35 課**依官方手冊 v1.3.0 (RevA) 撰寫。按鍵組合、選單位置、參數名稱都與官方對得上，但「按下去實際發生什麼」只有實機能確認

**建議優先順序：3-2 Resample 疊層法。** 它是全站最重要的一課，也是 Season 3 之後所有內容的地基。那一課若有出入，往回改的範圍最大。

---

## 4. 網站地圖

```
/                              首頁
/start                         ★開始之前（開箱、接線、充電、音量安全）
/learn                         課程地圖（五個 Season）
/learn/[season]/[slug]         ★課程播放器，例：/learn/s3/resample-layering
/genre                         曲風工廠總覽（L1–L4 分級）
/genre/[slug]                  單一曲風配方卡
/reference                     查詢區索引
/reference/specs               ★官方規格總表
/reference/official-order      ★官方順序對照（手冊章節 → 本站課程）
/reference/techniques          ★延伸技巧（官方依據／社群做法分標）
/reference/shortcuts           快捷鍵總表
/reference/knobs               ★互動式旋鈕矩陣
/reference/fx                  效果字典
/reference/glossary            名詞對照
/reference/troubleshoot        疑難排解
/reference/firmware            韌體對照與尚未驗證清單
/samples                       素材庫
/about                         關於本站（凡圖有限公司、事實查核、商標聲明）
/privacy                       隱私權政策
/offline                       離線備援頁（Service Worker 用）
```

課程網址採 SEO 英文 slug，驗證腳本會強制格式並**擋掉單獨的 `mpc`**（繁中「MPC 教學」搜尋結果前十筆幾乎全是 MPC-HC 播放器）。

---

## 5. 技術棧

| 層 | 選擇 | 實際版本 |
|---|---|---|
| 框架 | Next.js App Router + Turbopack | **16.3.0** |
| UI | React / React DOM | **19.2.8** |
| 語言 | TypeScript strict | **5.9.x** |
| 樣式 | Tailwind CSS | **4.3.3** |
| 搜尋 | Pagefind | **1.5.2** |
| 輸出 | `output: 'export'` 全站靜態 | — |
| Node | — | 22.15.0 |

**沒有安裝也不會安裝**：任何動畫函式庫、任何 webfont（含 Inter）、shadcn/ui、任何資料庫或 CMS、任何帳號系統。
面板動畫全部是 CSS class 切換，字體是系統堆疊。

---

## 6. 開發指令

```bash
npm run dev
```

```bash
npm run build
```

```bash
npm run check
```

| 指令 | 做什麼 |
|---|---|
| `dev` | 開發伺服器 |
| `build` | 建置。**會先跑內容驗證與面板比對，任一失敗就中止** |
| `check` | 型別 + 內容驗證 + 驗證器自我測試 + 面板檢查，一次跑完 |
| `stats` | 印出本檔 §2 的內容統計 |
| `videos` | 影片覆蓋率報告：哪些課或曲風還沒有影片 |
| `keys` | 按鍵圖示比對稽核：哪些詞會被畫成按鍵、有沒有誤判 |
| `videos:verify` | 用 YouTube oEmbed 逐一確認影片還在、標題沒寫錯 |
| `icons` | 重新產生 PWA 圖示 PNG |
| `typecheck` | TypeScript strict 檢查 |
| `validate` | 內容驗證 |
| `validate:selftest` | 驗證驗證器本身有沒有效 |
| `check:panel` | 面板行為 + 與原型座標比對 |

---

## 7. 品質防線

這個專案有四道自動關卡，**任何一道失敗都會中止 `npm run build`**。

### ① 內容驗證 `scripts/validate-content.ts`

- 所有 `targets` 必須是合法控制項 ID
- 每步必須有 `screen` 與 `hear`
- 每步 `say` 至少一個 `<b>`
- **單句不超過 40 字**（以全形句號、問號、驚嘆號斷句，不計空白）
- `outcome` / `checkpoints` / `firmwareVerified` / `verifiedDate` / `needsComputer` 非空
- `prerequisites` 必須指向存在的課程；`ch` 必須在章節範圍內
- slug 格式、不得含日期前綴、不得單獨用 `mpc`、同季不得重複
- 曲風八段式結構齊全；**L4 必須填寫 `limitation`**
- **曲風效果配方引用的效果名稱，必須真的存在於效果字典**

### ② 驗證器自我測試 `scripts/validate-selftest.ts`

拿一份刻意寫錯的資料去餵，五種錯誤必須全部被抓到；再拿一份完全合法的資料，必須零誤報。
**「驗證器有效」本身是可重跑的測試，不是口頭保證。**

### ③ 面板行為檢查 `scripts/panel-smoke.tsx`

每個 ControlId 都畫得出來、`r_` 前綴自動切背板、`pads` 展開成 16 顆、SHIFT 全面板紅字、
**同一個波形種子每次渲染結果必須完全相同**（否則 SSG 與 hydration 會對不起來）。

### ④ 面板移植比對 `scripts/panel-parity.ts`

直接讀 `reference/prototype-v2.html` 的原始碼，把每一個 `btn()` / `knob()` 呼叫的座標抓出來，
跟 `panel-layout.ts` 逐一比對：**按鍵 23、旋鈕 7、打擊墊 16、背板接口 8，座標與面板字全部相符**。
這保證面板是「移植」的，不是憑印象重畫的。

---

## 8. 事實查核政策

**唯一權威：Akai 官方使用手冊 v1.3.0 (RevA)。**

來源優先序：官方手冊技術規格 > 官方支援知識庫 > 官方 FAQ > 媒體評測（僅限使用感受）> 社群與影片（**可交叉比對與延伸觀看，不可單獨作為事實依據**）。

影片與官方衝突時**一律以官方為準**。只有影片支持、官方沒寫的，標「社群普遍做法，尚未驗證」。

### 已查證釐清的重點

| 項目 | 結論 |
|---|---|
| Windows 10 例外 | 一般用瀏覽器連 `mpc-sample.local`（Mac／Linux／USB-C 的 iOS 都可）。**Windows 10 需另外下載官方桌面更新程式**，並用 CHOP + MUTE + SAMPLE SELECT 開機進 Update Mode |
| Pad FX vs Knob FX | **Pad FX 套整段序列**，能指定單一 pad 的是 **Knob FX** |
| Flex Beat 幾種 | PAD 1 是 EMPTY 不做事，效果在 **PAD 2–16** |
| 電池 | 手冊寫「約 5 小時連續播放」，FAQ 寫「up to 6 小時」→ 依優先序**採 5 小時** |
| 匯出路徑 | Song 頁 B1 匯出 → B2 音訊混音 → ENCODER 命名 → B3 執行 |
| 傳檔路徑 | Project → SD Card Access，microSD 掛載成外接磁碟。**手冊明寫 microSD 不含在盒裝內** |
| 側鏈 | 本機沒有側鏈，用 Knob FX 的 **Pumper**（官方描述即為「類似側鏈壓縮」） |
| MIDI | MIDI Port 為 **External 或 USB 二選一**；背板為 1/8" TRS **Type A** |
| 序列切換 | 播放中按另一顆 pad 會**閃亮綠排隊**，等目前序列跑完才切換（已解決原待驗證項） |
| 取樣啟動 | **敲 pad 即開始錄**；錄音中再敲其他 pad 可當場切片；停止方式決定停完在哪個模式 |
| AUDIO IN | 2 個 **1/4" TRS，Mic/Line-Level**；唱盤必須先過唱頭放大器 |
| Takeover | **三種模式**：Pickup、Scaled、Instant。**K1–K3 預設 Scaled**（會動，速率縮放），**只有推桿預設 Pickup**（對上才動）。設定在 **MIDI Config**，不在 Fader 選單。本站原寫「旋鈕要對上才會動」為錯誤，2026-09-20 更正 |
| Loop Lock | Sample Mode 按住 SHIFT + B1 切換。開著時循環起點鎖在樣本起點，關掉才能各自獨立設定 |
| 盒裝內容 | 手冊 p.3 逐字：MPC Sample、USB-C 線、快速入門指南、安全與保固手冊。**其餘一律另購**，連接圖直接標「Adapter not included」 |
| Chop 幾種 | **三種**：Threshold／Regions（4、8、16）／Manual。本站原寫「四種」，2026-08-10 更正 |
| Recall 秒數 | **音訊 25 秒**；序列 Recall 撈的是「上一個循環」不是秒數。規格表的 30 秒與內文不符 |
| 沒有記憶卡 | **拿不出作品**。掛載到電腦的是 microSD，不是內建 8 GB |
| MIDI 轉接線 | **盒裝沒附**，要自己買 TRS Type A |
| 複音數與容量 | **32 個立體聲聲音**；每專案 16 樣本 × 8 bank、16 序列 × 8 bank，專案數無上限 |
| 可匯入格式 | .wav、.mp3、.aif／.aiff、.snd、.s1s、.s3s、.flac、.ogg |
| 錄音規格 | 24-bit／44.1 kHz；內部處理 32-bit 浮點；序列器 960 PPQ |
| 充電 | 視線材與電源而定，**可能只在關機時充得進去**；官方建議至少 5V 2A |

### 尚未驗證（6 項，前台公開列出）

microSD 容量上限與格式、喇叭是否單聲道、內建儲存是否 eMMC、韌體更新是否影響既有專案、
Splice 到底怎麼搭配這台機器、**官方列出但手冊完全沒說明怎麼操作的三個功能**
（lazy-chopping、auto-snapping、fixed-length sampling）。

**2026-08-10 排除 4 項**：Recall 秒數、沒有記憶卡能否取出作品、MIDI 轉接線是否附、電池小時數。
每一項的結論與依據都列在 `/reference/firmware` 的「已排除的疑問」，不是默默刪掉。

**這些在 `/reference/firmware` 頁面對讀者公開。看到「尚未驗證」就代表我們還沒實測，不是定論。**

---

## 9. 目錄結構

```
├─ CLAUDE.md                       作業指令
├─ docs/PROJECT-PLAN.md            專案唯一真相來源
├─ reference/prototype-v2.html     視覺真相來源（勿修改）
├─ app/                            路由與頁面
├─ components/
│  ├─ mpc/panel-layout.ts          ★所有面板座標，集中一處
│  ├─ mpc/MpcPanel.tsx             ★核心元件，本身零座標
│  ├─ lesson/                      播放器、註解框、章節條、音檔
│  ├─ genre/                       配方卡與迷你播放器
│  ├─ badges/                      韌體與需要電腦標籤
│  └─ reference/                   快捷鍵表、旋鈕矩陣、效果字典、搜尋
├─ content/
│  ├─ lessons/s1-01.ts … s5-06.ts  36 課
│  ├─ genres/l1–l4.ts              24 張配方卡（依難度分檔）
│  ├─ videos.ts                    影片對照表：官方 30 支 + 社群 63 支
│  ├─ reference/                   查詢區資料
│  └─ __fixtures__/                刻意寫錯與完全正確的測試資料
├─ scripts/                        驗證與檢查腳本
├─ types/{lesson,genre}.ts         資料契約
└─ styles/globals.css              設計 token
```

---

## 10. 法律紅線

- ❌ 不改寫或翻譯官方手冊（規格數字可引用，操作說明自行撰寫）
- ❌ 不轉貼論壇貼文原文
- ❌ **不做逐字稿、不做整份翻譯字幕、不做跟原片等長的文字版**（那是重製與改作）
- ✅ **（2026-08-10 放寬）看過影片的人可以用自己的話寫 200 字內的中文摘要**（`summary` 欄位）。
  驗證腳本強制：有 `summary` 就必須 `reviewed: true`。**Claude 看不了影片，所以不會產生任何摘要**
- ❌ 不下載影片或抽取其音訊當素材
- ❌ 不提供他人音樂素材下載（素材一律自錄或 CC0，保留原始檔佐證）
- ❌ 不教導破解或第三方韌體
- ✅ **（2026-08-09 解除）技術教學可指名串流平台為取樣來源**，但每處都要附版權提醒並連到 2-2

課程 **2-2「這段素材可以用嗎」** 完整處理取樣法律：兩層權利、Pelham 案（歐盟法院 2019，兩秒鼓組取樣也算重製）、
平台服務條款是獨立責任線、三種情境風險分級、四種乾淨來源，並明確聲明**本站不提供法律意見**。

---

## 11. 版控與部署

- GitHub private repo，預設分支 `main`
- 一個 Phase 一個 commit，格式 `P0: 階段名稱`，過驗證閘門才 commit
- **絕不進版控**：`.env.local`、任何 API key、R2 憑證。金鑰若誤入版控須立即輪替，不可只刪 commit
- 部署 Vercel，全站靜態輸出，`out/` 目錄

### Commit 紀錄

```
153c7be  P3 完成: Season 2 取樣工藝補齊六課
f1439f4  P5: Season 4 聲音設計與 Season 5 現場演出
28be0dc  P4: Season 3 編曲九課
84055ca  P6: 曲風工廠與素材庫
679b0e9  P3(部分): Season 2 前兩課與音檔管線
3abaa77  P2+: 補齊查詢區資料缺口
bb568b8  P2: 查詢區六頁與 Pagefind 搜尋
47cc982  P1: 面板元件與 Season 1
b280f85  P0: 專案初始化
```
